var pickHand = {};

pickHand.deal = function(){
	console.log("deal hand");
	// ask the server to shuffle decks and deal hands
	// show the dealt hands
	// allow players to pick cards to mulligan
	// send choices to server
	// server sends back final starting hand
	// start gameplay (g.state = gameplay.startTurn)
}

module.exports = pickHand;